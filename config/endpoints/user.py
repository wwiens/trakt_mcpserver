"""User endpoints."""

from typing import Final

USER_ENDPOINTS: Final[dict[str, str]] = {
    # User endpoints
    "user_watched_shows": "/sync/watched/shows",
    "user_watched_movies": "/sync/watched/movies",
}
